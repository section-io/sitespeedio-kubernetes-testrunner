apiVersion: batch/v1
kind: Job
metadata:
  name: sitespeedio
spec:
  template:
    metadata:
      labels:
        app: sitespeedio
    spec:
      containers:
      - name: sitespeedio
        image: ${image}
        args:
${args}
        - --graphite.host=graphite.${client_namespace}
        securityContext:
          privileged: true
      restartPolicy: Never
